version: '3.8'

services:
  dev-ssh-v1.1:
    image: devkitpro:v1
    container_name: devkitpro-ssh-v1
    ports:
      - "1888:58791"
      # - "59301-59310:59301-59310"
    # network_mode: "host"
    volumes:
      - /data/sshdata:/root/data
      - /h/workspace/codes/switch:/ws
    restart: unless-stopped
