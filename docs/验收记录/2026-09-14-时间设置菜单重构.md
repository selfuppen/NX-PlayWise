# 2026-09-14 时间设置菜单重构验收

## 范围

本轮一次性落地方案 A：家长区改为“今日调度 / 时间计划 / 离线加时 / 安全与偏好 / 支持与恢复”五个顶级页签；时间计划使用五卡中枢，周计划、国家节假日和 bedtime 使用一级全屏编辑页。额度优先级保持“今日临时 > 日期计划 > 国家节假日 > 周计划”，bedtime 继续作为并行硬限制。

实现保持 `rules.json version:2`、`set_bedtime_policy` 和 `skip_bedtime` 请求格式不变。状态结果仅增加可选 `bedtime.skipped_window`，旧结果按不可用解析。完整 bedtime 草稿覆盖七天窗口、节假日/调休规则和独立日期覆盖，并以 `activation:"immediate"` 整体保存。

## 自动化验收

- `python tools/test.py`：通过。
- `python tools/package_remote.py`：通过。门禁完成 C/Python/UI 测试、标准版/Device Lab/Eden 构建、预览和 Zip 校验。
- `python tools/package_remote.py --only previews`：通过，共验证 240 张预览并同步 34 张使用指南图片。
- 新增或更新的覆盖包括五页导航、支持顶级页、时间计划子页与触摸命中、六张今日卡、快速加时确认、bedtime 完整规则边界、`skipped_window` 新旧结果兼容和浅/深色预览矩阵。

候选产物：

- `playwise-2.1.1-alpha.zip`：`D998A8BD23C3544D9D103642EF793B0D8B4330D98EC012447F3615ADB16504A1`
- `playwise-complete-2.1.1-alpha.zip`：`253CF8BD173D19DE9D4A1698A48AE857F0B988043A892907C82F5B0DB5DFACAF`
- `playwise-device-lab-2.1.1-alpha.zip`：`1DE086620C147CEBED69CEC4F4749B3E12FB999745FFC24B6BD80CAA5792ED23`

## 仍需真机执行

以下项目不能由 host、Eden 模拟 PCTL 或预览替代，当前不得标记为通过：

- 在 Eden 实机 NRO 完成五页循环、五卡中枢、三个一级编辑页、快速加时确认/取消、草稿离页保护及全部触摸流程。
- 在真实 Switch 候选包验证提前跳过今晚后本窗口不限制、次日正常恢复、旧实例拒绝不误跳、首次立即启用的官方暂停设置与 Overlay 风险提示、以及 Overlay 恢复入口。
- 绑定候选 Zip 哈希记录掌机/底座模式的浅色与暗色截图、手柄焦点、共享字体和远距可读性。

Bedtime 真机资格结论继续为 `pending`；本轮没有修改 PCTL 私有命令、参数单位或 `0x44` 布局结论。
