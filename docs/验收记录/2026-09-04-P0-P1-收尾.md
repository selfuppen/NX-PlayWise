# P0 + P1 收尾复查

日期：2026-09-04。承接 [上一轮验收](2026-09-04-P0-P1.md)，范围仍限于 Companion NRO；P2/P3、家长网页和 Overlay 不在本次修改范围。

## 修正内容

- 生成页将“此码增加”改为“代码时长”，明确实际增加以兑换结果为准，避免每日上限场景下承诺面额全额增加。
- 兑换成功结果超过 120 秒或时间戳不可用时隐藏旧剩余，显示“状态待确认”；可核对的历史实际增加仍保留。
- 恢复失败结果按后台错误码给出下一步，覆盖已使用、日期不符、校验失败、冷却、存储和控制异常；不再统一建议重输旧码。失败不恢复已使用或过期代码的有效性。
- 等待结果使用中性提示。保留现有返回、PIN、长按确认和请求恢复流程。
- 修正测试指南遗留的 NRO 30 秒警示标准；浅深主题预览分别从同一示例基线开始，避免上一主题的末尾状态污染下一主题。

## 验证

- `python tools/test.py`：通过，日志 `build/p0-p1-followup-host-test.log`。
- `python tools/package_remote.py`：最终通过，日志 `build/p0-p1-followup-package.log`。包含 C/Python/UI 回归、标准与 Lab 组件构建、标准/完整/Device Lab Zip 校验。
- `git diff --check`：通过。
- 已生成 52 张浅深预览，输出 `build/ui-previews/`；新增上限、过期、120 秒边界及具体失败场景。目视核对生成上限、旧成功结果、已使用码、日期不符、存储失败及等待页，未发现文字与按钮重叠。
- 前两次 package 执行在新增 C 测试处失败：先是手写结果缺少必需 `state`，随后是误判结果构造器的成功返回值。测试已改用现有 `ptc_result_error_json` 构造合法后台结果，并按 0 表示成功断言；修正后完整门禁通过，生产协议未修改。

| 场景 | 浅色 | 深色 |
| --- | --- | --- |
| 生成代码与上限截断 | [浅色](../images/usage/p0-p1-followup/grant-capped-light.png) | [深色](../images/usage/p0-p1-followup/grant-capped-dark.png) |
| 旧成功结果与过期剩余 | [浅色](../images/usage/p0-p1-followup/redeem-old-result-light.png) | [深色](../images/usage/p0-p1-followup/redeem-old-result-dark.png) |
| 已使用码的下一步 | [浅色](../images/usage/p0-p1-followup/redeem-used-light.png) | [深色](../images/usage/p0-p1-followup/redeem-used-dark.png) |

## 真机验收

**待执行。** 本次未操作真实 Switch，不把 host 绘制、输入模型和编译测试记作真机通过。继续使用上一轮的手柄、触控、共享字体、长按移出/松开、生成旧码后调整时长、兑换成功/失败和中断恢复清单，浅深主题分别记录。

补充核验：代码面额与每日上限截断结果是否清楚；120/121 秒的余额状态是否一致；已使用或日期不符时是否能按提示完成换码；从等待或失败页返回后能否继续原流程。绑定本轮标准 Zip 哈希、机型、HOS、Atmosphère 与照片后再登记结果，资格仍为 pending。

## 本轮候选产物

以下哈希标识本轮通过构建门禁的候选，不表示真机验收通过。产物位于 `build/packages/`。

| 文件 | SHA-256 |
| --- | --- |
| `playwise-2.0.2-alpha.zip` | `a46746389faf549f44c8f773a3856935e6265b0c9283ac2dfec15e1c1fc8f4b2` |
| `playwise-complete-2.0.2-alpha.zip` | `b9f3c83cd4ed1079f1cd8cdc5c89291500664e4f02f905bd87695c0e16dbb710` |
| `playwise-device-lab-2.0.2-alpha.zip` | `8241845d7fd935c75cc2a5abc7a0acdb1ebca11426535b1a62693fc840e7bb76` |
