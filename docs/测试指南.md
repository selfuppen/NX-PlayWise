# 测试指南

项目只保留一个本地 Python 测试入口、一个本地容器编译打包入口和一次真机快速测试。协议拒绝路径、nonce、backup gate、PCTL 失败和恢复细节由自动测试覆盖，不再要求每次在真机逐项重做。

## 本地测试

Windows PowerShell：

```powershell
python .\tools\test.py
```

成功时只输出：

```text
PASS: local tests
```

该命令无第三方依赖，覆盖：

- token v1、observe queue、Web 辅助界面和容器打包工具回归。
- `protocol_probe` 的 init、离线码、observe 处理、result 和队列归档。
- observe 不消费 nonce。
- safe package 布局、默认 observe 和无 `boot2.flag`。
- 缺少 sysmodule 时禁止生成 boot2 package。
- boot2 package 中的 `pctc.ovl` 必须包含有效的 NRO asset header、`PCTC` 标题和 NACP 版本元数据，确保 Ultrahand 能枚举显示。
- request id 安全、文件名/嵌入 ID 一致性、IPC accepted-before-execute、fallback 不重复提交、扫描退避、分钟调度、跨日日志与 30 天清理。

当前日期日志位于 `logs/YYYY-MM-DD/`（UTC+8）。升级遗留的根级日志移动为当天目录中的 `legacy-<启动时间>-*`。

只有排查具体失败时才直接运行 `tests/` 下的单个脚本。

## 本地容器编译打包

涉及 C、Makefile、Companion NRO、sysmodule 或 package 的改动，确保 devkitPro 容器已经启动，并满足：

- SSH 地址为 `root@127.0.0.1:1888`。
- 当前仓库直接挂载到容器内 `/ws/switch-play-time-control-local`。
- OpenSSH 可以使用已配置的密钥登录；未配置密钥时运行期间会交互询问容器密码。

然后直接对当前工作区运行：

```powershell
python .\tools\package_remote.py
```

脚本通过一次本地 SSH 会话完成以下工作：

1. 清理挂载仓库中的旧 `build/packages/` 结果。
2. 在挂载仓库中运行完整 C/Python 测试，不要求提交或推送当前改动。
3. 构建 Companion NRO、sysmodule NSP 和全部 package。
4. 直接在挂载工作区生成并校验五个 zip：`safe-nro`、`disabled-boot2`、`observe-boot2`、`grant-boot2`、`enforce-boot2`。

四类 boot2 包还会编译并包含 `switch/.overlays/pctc.ovl`；`safe-nro` 不包含 overlay，也不依赖 Tesla。package 不捆绑 nx-ovlloader 或 Tesla Menu。

使用 `tools/install_package_to_sd.ps1` 安装解压后的 package 时，脚本会完整复制并校验 `switch/play-time-control`、项目 sysmodule 内容目录和可选的 `switch/.overlays/pctc.ovl`。切换到不含 overlay 的 safe package 会移除旧 `pctc.ovl`，但不会删除 `.overlays` 目录或其他第三方 overlay。

目标盘符默认是 `E`，SD 卡挂载到其他盘符时用 `-Drive` 指定，接受 `H`、`H:` 或 `H:\` 三种写法：

```powershell
.\tools\install_package_to_sd.ps1 -SourceFolder <解压目录> -Drive H -Apply
```

脚本会先确认该盘符是存在的文件系统盘、拒绝源目录位于目标盘，并在 `-Apply` 时要求输入目标盘根路径（例如 `H:\`）确认删除旧安装。

产物目录（容器和本机是同一个挂载目录）：

```text
<仓库>\build\packages
```

SSH 地址、端口、用户、挂载路径或私钥不是默认值时，可使用 `--host`、`--port`、`--user`、`--container-path` 和 `--identity` 覆盖。

脚本开始前会清理 `build/packages/` 中的旧结果；容器测试或构建失败时不会产生新的完整 package 集合。

## 图形界面验收

在模拟器或真机以 1280×720 检查以下内容：

- 孩子首页能显示今日状态、剩余时间、已玩时间、控制模式、计时器、就寝限制、临时解锁和最近反馈，且不暴露 request id、路径或原始 JSON。配置额度和剩余时间均可用时显示“约 N 分钟”，否则显示暂不可用。
- “今日管理、时间计划、安全工具”三页共 16 张操作卡完整可达，分别为 6、5、5 张，两列网格；`L/R` 切页、方向键选卡、`A` 执行、`B` 返回、`Y` 刷新。
- 触摸点按与手柄等价：标签页切页、卡片直接执行、底部输入/刷新/退出/切页/返回按钮、弹窗确认取消、分钟加减与中央数值、七日编辑器的星期、模式和“输入分钟”、就寝字段与增减、限制方式三选项。绘制与命中测试共用同一套控件几何，点击区域与显示控件一致。
- 今日管理页展示今日状态面板；安全工具页展示写入验证、效果验证、强制阻止和暂停软件四项能力状态。
- 点按“验证强制阻止”或“验证暂停软件”会真实执行对应探针。两者在失败时都不产生 `flags/disable.flag`，只有强制阻止探针在恢复失败这一种情况下才会创建该文件。这可用于区分能力探针失败与快速设备测试失败。
- 分钟步进器、七日编辑器、就寝时间编辑器、限制方式三段选择器和全部确认弹窗没有文字越界、焦点不清或控件重叠。
- PIN 使用密码软键盘，离线码使用下载码软键盘，分钟数使用数字键盘；取消软键盘、输入非纯数字或超范围数字、取消编辑器或危险确认均不创建请求。今日额度和临时解锁允许 1–1440，临时加时允许 1–120，每周每日限额允许 1–1440。
- 孩子页不显示家长区快捷按钮，触摸底部区域不能进入家长区，`Minus` 也不能进入；只有长按 `L+R+X` 后通过本地 PIN 才能进入。
- 简体中文共享字体正常加载，所有中文无乱码；等待、成功、拒绝和超时状态不会引发布局跳动。
- 后台超时、坏授权码、缺少状态和快速设备测试失败均显示简短中文反馈；快速设备测试失败后存在 `flags/disable.flag`。
- 图形、共享字体或 FreeType 初始化失败时只显示最小中文恢复页，并可从该页退出。

模拟器可验证页面和请求流程，但共享字体兼容性、PCTL 效果及快速测试后的真实恢复仍以真机结果为准。

## 真机快速测试

日常真机验收只运行快速 PCTL effect probe，不再执行 A-F 阶段、拒绝用例矩阵、手工 1 分钟授权码或 enforce snapshot。

前置条件：

- 已掌握 SD 卡读写方式和官方家长控制 PIN/App。
- 知道如何创建 `sdmc:/switch/play-time-control/flags/disable.flag` 和移除 `boot2.flag`。
- 本地容器编译打包已通过。

操作：

1. 将最新 `grant-boot2-<timestamp>.zip` 解压到 SD 卡根目录并重启进入 Atmosphere。
2. 启动 Companion，进入“安全工具”，选择“快速设备测试”，阅读风险说明后按 `A` 二次确认。
3. 等待最近反馈显示“快速设备测试通过，设置已自动恢复”。

一次快速测试自动覆盖 request queue、真实 PCTL 会话、写前备份、目标写入、StartPlayTimer、运行时状态读取、能力持久化和原始设置恢复。PASS 必须同时满足：

- result 为 `probe_play_timer_effect`、写入模式、非 dry-run。
- `pctl_effect_probe.verdict` 为 `pass`。
- raw settings 和 timer enabled 状态均已恢复。
- `play_timer_write_verified` 与 `play_timer_effect_verified` 均已持久化。
- backup、`effect_before` 和 `effect_restore` 证据存在。
- `opaque_snapshots` 显示 active 只改变目标日期的分钟字段，restored 与 before 完全一致。
- 当天已有累计游玩时间时，快速探针仍应使用 1440 分钟或保留至少 1 分钟剩余的安全临时目标，不应短暂触发“限制时间到期”。

Layout v2 真机复测必须使用全新的 `grant-boot2` package。旧 `pctl-s-v1` / `pctl-s-runtime-v1` capability 会自动失效，不得手工改回 true。若 backup 的 slot 摘要不是 `h:...;d0:flag=...,enabled=...,m=...` 格式，说明仍在运行旧 sysmodule，应停止测试并重新安装 package。

失败、超时或自检不通过时，Companion 自动创建 `disable.flag` 并显示 `FAIL`。不要继续 grant、enforce、raw block 或 suspend；保留 result、logs、capabilities 和 backup 排障。

## Tesla 到时入口验收（HOS 22.5.0）

这是覆盖层方案的停止门槛，不能由构建成功替代。前置安装与当前 Atmosphere/HOS 匹配的 nx-ovlloader 和标准 Tesla Menu，并移除具有管理或文件操作能力的无关 overlay。

1. 正常状态用标准 Tesla 热键打开菜单，选择 PCTC；确认简体中文字体、标题、8 位码槽和大号 3×4 数字键盘无乱码，键盘、状态卡片和 Tesla 底栏不重叠。方向键单击与按住连续移动均正常，同时按多个方向时焦点不移动；A 输入、X 删除、Y 清空、Plus 提交、B 关闭均可用，删除和清空不改变键盘焦点。未满 8 位时提交按钮显示禁用状态，满 8 位后明显高亮，提交请求不会弹出系统软键盘。当前版本不验收触摸输入。
2. 触发官方游玩时间硬限制弹窗；确认 Tesla 菜单和 PCTC 仍能显示并接收方向键、A、B、Plus。任一项失败就停止，不继续产品化，也不修改 qlaunch。
3. 输入有效码；result 必须为 `offline_code`、`status:"ok"`、`dry_run:false`，`play_timer_enabled:1`、`restricted_now:0`，剩余分钟增加。官方弹窗必须消失，覆盖层关闭后返回原游戏；否则真机验收失败。
4. 无效、过期、重复、额度不足、sysmodule 未运行、`disable.flag` 或 PCTL 失败均停留在错误页，不消费 nonce；`observe` 只显示演练，不提示已解锁。
5. 重复掌机/底座、睡眠唤醒和重启场景。固件或 Atmosphere/nx-ovlloader 版本改变后重新执行本节。

6. 分别验证三种传输状态：IPC 可用时显示“传输：IPC”；IPC 不可用时显示“传输：SD 文件队列”；IPC 已 accepted 但事件失效时显示“传输：IPC → SD 结果回读”。三种情况都不得重复提交同一个 request。
7. 无效码、重复码、后台超时和 observe 演练均显示中文状态卡片；错误信息位于 Tesla 底栏上方，必要时显示“错误码：N”，不得显示原始英文 reason 或文件路径。

## 快速测试之后的功能验证

快速设备测试只证明底层写入被 PCTL runtime 采纳，不证明业务链路。确认快速测试通过后，按下面三个阶段依次验证，任一阶段不通过就停在该阶段排查，不要跳到下一阶段。

### 阶段 1：grant 模式基础功能

沿用已安装的 `grant-boot2` package，不需要换包。

前置条件：

- `capabilities.json` 中 `play_timer_write_verified` 与 `play_timer_effect_verified` 均为 `true`，且 backend 分别为 `pctl-s-v2` 与 `pctl-s-runtime-v2`。
- 不存在 `flags/disable.flag`。

操作与通过判据：

1. 家长区“今日管理”选择“设置今日额度”，设为一个明确值（例如 40 分钟）。result 为 `status: ok`、`dry_run: false`；`rules.json` 中 `today_override_present: true`、`today_override_minutes` 等于该值；主机系统设置或 Nintendo Parental Controls App 中当天限额同步变化。
2. 在 PC 生成离线加时码，当天使用可省略 `--date`：

   ```powershell
   python .\tools\grant_code.py --minutes 15 --device <device_id> --secret <grant_secret>
   ```

   `--device` 和 `--secret` 必须与 SD 卡 `config.json` 一致。在孩子首页按 `A` 或点按输入按钮录入该码。判据：`today_override_minutes` 变为 `40 + 15 = 55`，即在当日额度上**累加**而不是覆盖；官方界面同步；`ledger/used_nonces.jsonl` 追加一条记录。
3. 把同一个码再输入一次。必须被拒绝，错误 reason 为 `used_token`；`ledger/used_nonces.jsonl` 不再新增，`rules.json` 不变。

### v2 数字短码

使用 `python tools/grant_code.py --tier-minutes 30 --device <id> --secret <secret>`
生成 8 位数字短码。Overlay 和 Companion NRO 会显示数字键盘；输入一位后自动前进，
`X` 退格、`Y` 清空、`Plus` 提交。连续 5 次 v2 格式或签名错误后，短码进入 10 分钟
冷却，状态保存在 `state.json`，重启不能绕过；成功写入并保存结果后清零计数。
4. 生成一个 `--minutes` 大于 `config.json` 中 `max_add_minutes` 的码并输入。必须被拒绝，reason 为 `minutes_exceed_limit`，且不消费 nonce。
5. 生成一个 `--date` 为昨天的码并输入。必须被拒绝，reason 为 `wrong_date`。
6. 依次执行“临时加时”“今日不限”“恢复周计划”。三者 result 均为 ok；`rules.json` 中 `today_override_mode` 相应变为 `limit` 与 `unlimited`；执行“恢复周计划”后 `today_override_present` 变为 `false`。
7. 重启主机后再次查看孩子首页。今日额度与重启前一致，确认 SDMC 持久化生效。

失败处理：任一步 result 不为 ok 时，保留 `results/`、当天的 `logs/YYYY-MM-DD/` 和 `backups/`，按 reason 对照 `docs/协议.md` 的错误契约。出现 `pctl_write_not_verified` 或 `pctl_effect_not_verified` 说明对应 capability 已失效，需要重新运行快速设备测试。

### 阶段 2：不写 PCTL 的规则类功能

仍在 `grant` 模式。这些请求只更新本地规则，不经过 PCTL 写入路径，因此可用于区分故障出在 PCTL 层还是规则层。

操作：设置每周计划、设置就寝时间、限制方式选择“仅提醒”、执行“临时解锁”后再“结束解锁”。

通过判据：

- 全部返回 ok。
- `rules.json` 中 `week`、`bedtime_enabled`、`bedtime_start_min`、`bedtime_end_min`、`limit_action` 相应更新。
- `state.json` 中 `parent_unlock_until` 在解锁后为未来时间戳，结束解锁后回到 `0`。
- 解锁生效期间孩子首页显示“临时解锁：已开启”，结束后显示“未开启”。

当前能力边界，属于预期行为而不是缺陷：

- 限制方式选择“强制阻止”或“暂停软件”在对应能力探针通过前会被拒绝，reason 分别为 `raw_block_not_verified` 和 `suspend_not_verified`。
- “今日禁玩”在强制阻止能力验证前会被拒绝，reason 为 `raw_block_not_verified`。该 gate 位于控制模式判断之前，因此在 `observe` 模式下同样拒绝。

这三项都需要先运行阶段 4 的能力探针；探针通过并持久化 capability 后即可使用。

### 阶段 3：enforce 模式自动对账

前置条件：阶段 1 和阶段 2 全部通过。将 `enforce-boot2` package 解压到 SD 卡根目录并重启。

操作与通过判据：

1. 启动后等待约 15 秒启动延迟。后台在分钟边界自动把当天规则写入 PCTL 并调用 `start_timer`，不需要提交任何 request。当天 `logs/YYYY-MM-DD/events.jsonl` 出现 `pctl_start_timer` 和 `state_persisted`；`state.json` 中 `last_enforced_day_index`、`last_enforced_mode`、`last_enforced_minutes` 被填入。
2. 保持不操作继续观察数分钟。去重生效，不应重复写入，`events.jsonl` 不再出现相同的 `pctl_start_timer`。
3. 在家长区修改今日额度。下一轮 tick 自动对账到新值，`last_enforced_minutes` 跟随更新。
4. 跨日验证：次日首次启动或跨过本地 0 点后，自动按新一天的周计划重新写入，`last_enforced_day_index` 递增。
5. 创建 `flags/disable.flag`。tick 立即停止对账，`events.jsonl` 无新增写入；删除该文件后恢复对账。

失败处理：出现重复写入或跨日不切换时，保留 `state.json` 与对应日期目录的 `events.jsonl`。出现 `pctl_apply_failed` 时同时保留该目录的 `pctl_debug.jsonl`。

### 阶段 4：强制阻止与暂停软件能力探针

前置条件：阶段 1 已通过，`play_timer_write_verified` 与 `play_timer_effect_verified` 均为 `true`，不存在 `flags/disable.flag`，且处于 `grant` 或 `enforce` 模式。在 `observe` 下两个探针只返回 `dry_run:true` 与 `verdict:"not_run"`。

这两项是解锁“今日禁玩”和“强制阻止 / 暂停软件”限制方式的唯一途径，属于按需执行项，不在日常快速测试范围内。

#### 验证暂停软件

家长区“安全工具”选择“验证暂停软件”。该探针只读，不写 PCTL、不改设置。

通过判据：

- result `status: ok`，`suspend_verified` 为 `true`，`suspend_backend` 为 `pctl-s-suspend-v1`。
- `events.jsonl` 出现 `probe_ok`。

失败判据与含义：

- 返回 `pctl_write_failed` 且 detail 为 `suspension event unavailable`：本机固件未提供该事件，暂停软件不可用。
- detail 为 `suspension event ok but play timer alarm is disabled`：主机自身已关闭游玩计时器提醒，系统不会发出暂停请求。需在官方家长控制中启用游玩时间提醒后重测。

`suspend_verified` 只表示系统暂停请求通道可用。实际暂停由 Horizon 按主机自身的家长控制设置执行，本项目不自行暂停软件。

#### 验证强制阻止

家长区“安全工具”选择“验证强制阻止”。该探针会真实写入：把当天 weekday 写成 `minutes = 0` 的受限状态，确认主机运行时报告受限，然后恢复完整的 0x44 原始设置和原始 timer 状态。

风险与时窗：探针执行期间当天会短暂变为禁玩。正常情况下写入、校验和恢复在数秒内完成。若恢复失败，探针返回 `pctl_restore_failed`、清除 `raw_block_verified` 并创建 `disable.flag`，以免验证动作把主机长期留在禁玩状态。此时按“恢复”一节处理，并用官方 PIN 或 Nintendo Parental Controls App 确认当天限额已回到预期值。

建议在孩子不在使用主机时执行，并提前确认可以访问官方家长控制 PIN。

通过判据：

- result 为 `probe_raw_block`、写入模式、非 dry-run。
- `pctl_raw_block_probe.verdict` 为 `pass`。
- `checks.raw_target_written`、`checks.blocked_observed`、`checks.raw_restored`、`checks.timer_restored` 均为 `true`。
- `opaque_snapshots` 中 active 只把目标日期的分钟字段改为 0，restored 与 before 完全一致。
- `capabilities.json` 中 `raw_block_verified` 为 `true`，`raw_block_backend` 为 `pctl-s-rawblock-v1`。
- `events.jsonl` 出现 `raw_block_before`、`raw_block_apply` 与 `raw_block_restore`；`backups/last_pctl_backup.txt` 已更新。
- 不存在 `flags/disable.flag`。

常见失败 `failure_stage` 含义：

- `write`：主机拒绝 `minutes = 0`，说明 raw 0 不是本机可用的禁玩表示。
- `runtime_status`：写入被采纳但运行时未报告受限，禁玩语义不成立。
- `restore`：恢复未通过，已进入 fail-open，必须优先确认当天限额。

两项探针通过后，回到阶段 2 重测“限制方式”和“今日禁玩”，两者应不再返回 `*_not_verified`。

## 恢复

按以下顺序处理：

1. Companion 家长区“安全工具”选择“紧急停用控制”，二次确认后创建 `disable.flag`。
2. Companion 无法启动时，从 SD 卡创建 `sdmc:/switch/play-time-control/flags/disable.flag`，然后重启。
3. 需要完全停止 boot2 时，删除 `sdmc:/atmosphere/contents/4200000000BD2300/flags/boot2.flag`，或安装 `disabled-boot2` 包。
4. 官方游玩时间限制或暂停软件已生效时，使用官方 PIN 或 Nintendo Parental Controls App 解除。

恢复期间不要删除 `backups/last_pctl_backup.txt`、`results/` 或 `logs/`。确认问题解决且目标模式正确后，才能在 Companion 的“安全工具”中选择“恢复控制”并二次确认删除 `disable.flag`。

### sysmodule 崩溃排查

sysmodule 崩溃时 Atmosphère 会写入 `sdmc:/atmosphere/crash_reports/<tick>_4200000000bd2300.log`。判断栈溢出的方法：比较 `Crashed Thread Info` 的 `SP` 与 `Stack Region`，如果 `SP` 低于区间下界，即为主线程栈溢出而不是空指针（此时 `Fault Address` 常显示 `0`，容易误判）。`X[12]` 通常是该帧申请的字节数，`FP - X[12] == SP` 可确认是函数序言里越界。

用 `Module Info` 的偏移在 `sysmodule/build/pctc-sysmodule.map` 里定位符号：`PC` 落在崩溃函数的 `.text.<name>` 区间内，`LR` 落在调用者区间内。`sdmc:/switch/play-time-control/logs/<date>/sysmodule.log` 的最后一行可确认启动流程停在哪一步。

栈预算约束：`main` 的单例（`PtcIpcServer` 约 68 KiB、`PtcSysmodule` 约 10 KiB）必须保持 `static`，不要改回栈上局部变量；`PtcStorageEntry[256]` 约 39 KiB，`ptc_sysmodule_cleanup` 与 `cleanup_timestamped_json` 必须共用调用方传入的同一个数组，不得各自再声明一份。

## 能力边界

- 快速测试是日常真机权威入口，但不验证 raw block 或 suspend；两者继续受 capability gate 保护，需要按阶段 4 单独运行探针。
- 快速测试通过只解锁 play timer 限额写入路径，即离线加时码和今日额度类操作。`raw_block`、`suspend` 和 `block_today` 需要先通过阶段 4 的“验证强制阻止”与“验证暂停软件”探针。
- 强制阻止探针会真实写入并短暂使当天变为禁玩，恢复失败时进入 fail-open；不要在孩子正在使用主机时运行。
- `raw_block_verified` 和 `suspend_verified` 必须由探针写入，不得手工改为 `true`。缺少或不匹配 `raw_block_backend` / `suspend_backend` 时对应 capability 会在加载时自动失效。
- expiry probe 仅用于专项排障，不是普通验收项。
- `safe-nro` 和 `observe-boot2` 保留用于 UI 或只读专项排障，不属于日常流程。
- 请求字段、错误码和结果结构以 `docs/协议.md` 为准。
