# 测试指南

测试分为确定性 host 回归、devkitPro 容器构建和真机资格验证。标准分发构建的自动测试覆盖完整安全与恢复能力，但设备端不运行循环写入/回滚式 self-test。

## 本地回归

```text
python tools/test.py
```

通过标志为 `PASS: local tests`。该入口覆盖：

- token v1/v2、8 位码、日期、签名、replay、nonce 提交顺序和冷却；
- request/result schema、原子队列、IPC 去重、stuck request 恢复；
- setup 预检、snapshot、5 秒宽限、兼容状态和恢复优先级；
- 普通 PCTL 事务、失败注入、rollback、result/ledger 失败；
- Enforce 去重、`applied_pending_confirmation`、30 秒确认/回滚、已耗时长超过恢复后周额度且 1455 已恢复 false 时按精确设置回读提交、306 错误结果真实运行时快照的 UI/诊断保留，以及活跃 recovery 与启动遗留事务的区分；
- PIN、规则、安装模板幂等初始化、直接覆盖升级的数据保留和诊断脱敏；
- 标准安装包/完整交付包/Device Lab 契约隔离和 package helper 失败路径。
- `version.h`/`version.mk` 一致性，以及 PWA 官方链接和外链安全属性。
- 单文件离线版与模块化源码的一致性、内联 CSP、零运行时网络依赖、Web Crypto/内置 HMAC 固定向量和无持久存储降级。

测试必须使用固定时间、固定 fixture 和 host adapter，不依赖真实 Switch 服务。

## 权威容器验证

涉及 C、C++、Makefile、NRO、Overlay、sysmodule 或 package 时必须执行：

```text
python tools/package_remote.py
```

脚本在单次 SSH 会话中清理现有构建产物，运行 C host、UI 和 Python 回归，编译三个标准分发组件并生成标准安装包与完整交付包。

分发门禁断言：

- `build/packages/` 只有当前版本的标准安装包 `playwise-v<当前版本>.zip` 和完整交付包 `playwise-complete-v<当前版本>.zip`；
- 标准安装 Zip 只包含标准分发 sysmodule、NRO、Overlay、`boot2.flag`、`build.json`、不可变 `defaults/*.json` 及空运行目录，不包含六个根目录可变 seed、安装器、离线网页或校验专用文件；
- 完整交付 Zip 只包含字节一致的当前版本标准安装包与 `playwise-offline.html`，拒绝缺失、重复、额外、路径穿越或内容不一致的成员；
- Zip 内 `build.json` 与构建阶段外部生成的 release manifest 完全一致，profile 为 `release`；
- 三个二进制都嵌入同一 manifest，NRO/Overlay 均显示当前构建版本；NRO 还必须嵌入项目仓库和 PWA 地址；
- 无 LAB handler、标准协议外请求、运行时控制模式、`capabilities.json` 或占位 secret；
- 不 seed `credentials.json`，首次启动必须生成唯一 secret；缺失运行文件只从 `defaults/` 原子创建，已有文件即使无效也不得覆盖；
- 直接把新版安装包合并到已有目录以及使用 `tools/install_package_to_sd.ps1`，都必须替换程序和 build manifest，同时逐字节保留 credentials、PIN、rules、state、ledger、logs 和 backups；该脚本不打入运行时 Zip。

禁止用手工 SSH、`docker exec` 或复制回本地代替该入口。

## Device Lab 隔离验证

内部人员在 devkitPro 环境显式运行：

```sh
make device-lab-package
```

检查：

- 输出只在 `build/device-lab/`，不会出现在 `build/packages/`；
- Title ID 为 `4200000000BD23F0`，IPC 为 `pwtl:u`，SD 根目录为 `switch/playwise-device-lab`；
- manifest profile 为 `device-lab`；
- NRO 持续显示 DEVICE LAB 危险水印，操作要求 ZL+ZR+A；
- 包默认不含 `boot2.flag`；
- `make packages` 不会构建或携带任何 LAB 产物。

## Eden 模拟器快速测试

只想在 Eden 里快速看 UI 和业务流程时，在 devkitPro 环境显式运行：

```sh
make eden-test-nro
```

产物为 `build/eden-test/pctc-eden.nro`。Eden 只需加载该单个 NRO，不需要预先铺开安装包：首次运行会在 `sdmc:/switch/playwise-eden` 自行创建独立测试数据（含 `build.json`、`defaults` 之外的六个运行文件、固定测试 `credentials.json` 和假 `environment.json`），并在 NRO 进程内用模拟 PCTL 运行真实的 `sysmodule_core`。Eden 的 Windows 虚拟 SD 不保证跨目录 `rename` 的队列语义，因此该 profile 通过进程内 IPC 桥同步提交请求并照常持久化 result JSON；PIN 等模态界面不会阻塞处理，`status` 和 `complete_setup` 都会及时返回。真实 SD 原子队列仍由 host 测试和真机路径覆盖。

固定测试参数：设备 ID `eden-switch`，grant secret `playwise-eden-test-secret-00000001`。生成可兑换的加时码：

```sh
python tools/grant_code.py --tier-minutes 30 --device eden-switch --secret playwise-eden-test-secret-00000001
```

界面持续显示红色 `EDEN TEST` 角标，传输行固定显示"传输：Eden 模拟后台"。测试状态跨启动保留；需要重新走首次设置时只删除 `sdmc:/switch/playwise-eden`，不得删除或覆盖 `sdmc:/switch/playwise`。

该入口只覆盖初始化、PIN、页面导航、规则、离线码、nonce 和恢复流程；**不作为真实 PCTL 私有命令、0x44 layout 或计时效果的证据，也不替代真机资格验证。** Overlay 需要 Tesla，Eden 无法加载，因此浮窗仍只能在真机验证。

隔离断言：产物只在 `build/eden-test/`，`make packages` 与 `make device-lab-package` 均不构建也不携带该目标；`python tools/package_remote.py` 无条件扫描标准分发与 Device Lab 二进制，拒绝任何 Eden marker。需要顺带构建并校验该 NRO 时使用 `python tools/package_remote.py --with-eden`；不加该开关时默认构建的命令、耗时和产物集合完全不变。

## UI 实机验收

修改 NRO 或 Overlay 后，至少验证：

- 主题专项：在家长区“设置 → 外观主题”分别选择“跟随系统 / 浅色 / 暗色”，确认立即完整重绘且重启后保留；配置写入失败时恢复原主题并提示。旧 `config.json` 缺少 `theme` 或值无效时按“跟随系统”处理，系统主题读取失败则安全回退浅色，不能阻止 NRO 启动或改变任何控制状态。“跟随系统”在启动和从 HOME 菜单恢复后重新读取系统设置。OLED Hybrid 暗色逐页覆盖孩子页、首次设置、家长页、错误页和全部弹层；正文/次文字至少 4.5:1，焦点/功能边框至少 3:1；成功、警告、危险和中性状态同时使用几何符号、颜色与文字；二维码保持白底黑码且可扫码。Overlay 继续使用自己的固定深色受限色板，不读取 NRO 的主题偏好。

1. 使用没有 `config.json` 向导字段的全新配置，确认首次设置按“快捷键 → PIN → 外观主题 → 接管 → 进入区域”推进；中途退出后重启会恢复到已保存步骤。旧版未完成向导从第一步安全重启，已完成安装的步骤 `0` 保持不变；已经完成接管时不得重复提交接管请求，未完成前不允许普通写入。
2. 快捷键页固定显示 `Minus`，并提供 `L + R`、`ZL + ZR` 分别搭配四个方向键、`Plus`、`Minus` 在内的 14 项预设，不显示“录制其他组合”、录制触摸区域或 `X` 录制入口。选择只更新草稿，按 `+` 确认后才生效；未确认或明确关闭时仅保留 `Minus`。升级前已保存的合法非预设掩码不得被清除，且仍能解析和触发。首次快捷键页、区域选择摘要、孩子区页脚和家长区快捷键管理均须说明自定义组合需连续长按约 400ms，固定 `Minus` 松开即可进入且无需长按。分别真机验证 `L + R + 右` 的方向键和左摇杆右推连续按住约 400ms 后只触发一次、提前松开不触发、完全松开后可再次触发；组合候选按住期间不得穿透为孩子区普通输入。另验证固定 `Minus`、包含 `Plus`/`Minus` 的已保存组合及重启后的配置恢复。
3. 全新安装在认证状态合法且为空时自动创建默认 PIN `110`，认证文件只包含随机盐和哈希，不得保存明文；PIN 页明确提示弱保护，并提供“继续使用”和“修改 PIN”。覆盖修改默认 PIN、保留已有 PIN、已完成安装不覆盖、异常认证文件不覆盖和认证写入失败时停留本页并可重试。向导外的“修改任我玩PIN”仍必须先验证当前 PIN。PIN 输入另覆盖 1 位、6 位、64 位、65 位、非数字、两次输入不一致，以及少于 4 位只提示风险；所有 PIN 入口的错误都使用最上层“重试 / 取消”弹窗，错误后清空当前输入，两次不一致时清空两次输入。冷却弹窗逐秒更新剩余秒数并禁用重试，取消不计失败。摇杆 PIN 还需验证顺时针八方向映射、左右摇杆死区与单次锁存、十字键四方向、`X/Y` 的 0/9、触摸数字键盘、`ZL` 退格、`B` 取消以及短按/长按 `+` 的确认与临时系统键盘切换；方向示意中的 1 到 8 使用一致的主题强调色，不随数字键盘焦点产生异常变色，右侧四键圆阵列把 `X/0` 与 `Y/9` 标在对应圆内；已输入 PIN 只能显示掩码圆点。
4. 第 3 步可直接选择“跟随系统 / 浅色 / 暗色”，默认跟随系统并立即应用；原子保存失败时恢复原主题、显示错误并停留当前页。
5. 第 4 步接管确认前只做只读预检；确认后验证安装 snapshot、解除当前限制、约 5 秒同步宽限，并在宽限结束后进入第 5 步。对已有官方限时日，记录“总 180 分钟、已玩 60 分钟、剩余 120 分钟”向量：完成接管后必须仍剩余约 120 分钟（允许显示值相差 1 分钟），不得套用工作日默认 60 分钟；次日才切回 PlayWise 周计划。无法读取总额度且无法由已玩加剩余计算时必须返回 313、不得写 PCTL。制造旧后台与新版构建清单不一致的 `504 release_manifest_invalid`，确认向导提示完整重启主机后再试，并在仍失败时引导到 GitHub 项目 Issue 页反馈且包含致谢。
6. 第 5 步默认选中家长区，孩子区和家长区选项都显示简短切换说明；手柄或触摸选择区域后均不得立即进入，必须再触发独立的“确认进入家长区/孩子区”按钮。首次确认进入家长区不重复验证 PIN，之后从孩子区进入家长区需要 PIN。
7. 家长区“设置”页面存在“家长区快捷键管理”下级页，可选择预设或关闭自定义组合，并显示孩子区提示开关和当前状态；不得显示任意组合录制入口。关闭提示后孩子区不得显示固定 `Minus -`，也不得禁用任何已启用入口；“加时码”右侧帮助不得混入进入家长区或 PIN 说明。
7. 孩子首页以“自律小达人 · 加时奖励”和“遵守约定、合理安排时间”为标题；顶部的“系统计时器”与“运行状态”同列。加时码输入框默认以大字显示“输入加时码”，并显示护眼提醒；输入后以大字显示 8 位码。游戏时间统计在按游戏数据不可用时必须明确显示“游戏明细暂不可用”和真实的今日累计已玩时间，不得伪造前 3 名游戏排行；最近执行区域保持紧凑且不遮挡页脚操作。“最近执行”标题左侧状态框约为 `20×20`，在浅色、暗色主题下均不得与标题或下一行“命令”文字重叠。孩子区只使用 `B` 或触摸退出，`+` 不得作为退出快捷键，以免与家长区自定义组合冲突；图形初始化失败的备用界面可保留 `+` 退出。
8. 家长区顶级页固定为今日总额度、周计划、国家节假日、加时码、设置五类；加时码页只有三个生成入口，设置页提供外观主题、修改 PIN、快捷键管理、高级设置和支持与恢复。高级设置和支持与恢复都是完整二级页：顶栏继续高亮设置，B 和触摸返回设置，L/R 与顶级页签触摸均不得切页。高级设置正文顶部额外显示“设置 / 高级设置”层级条、“二级页面”标识和独立触摸返回入口，操作卡下移且不得与层级条或最近执行重叠。紧急停用、恢复事务、protection 或 failed 时，设置页签和支持入口显示“需处理”且入口为推荐项；首次接管未完成显示“待完成”；普通历史错误不点亮 Badge。三个重构页仍须显示与其他家长页一致的完整“最近执行”卡，正文、触控区域、状态卡和页脚不得重叠。国家节假日页使用左侧 760px 控制区和右侧 384px 状态区，总开关关闭时仍可编辑并保存两套未启用预设；关闭状态保存后不得声称今天已应用节假日规则。唯一的保存按钮横跨左侧控制区；无修改和保存中均不可重复提交。日历使用当前草稿预览并标明尚未保存，切页、返回孩子区或退出前默认聚焦“继续编辑”，放弃后才丢弃草稿。方向键必须与二维布局一致，并提供按节日分组的内置安排分页查看；未覆盖年份不得显示伪造安排。孩子区完全不显示自制程序菜单高级入口状态或快捷键 `X`。
9. “支持与恢复”的六张卡始终显示；页面正文顶部显示“设置 / 支持与恢复”层级条、“二级页面”标识和独立触摸返回入口，操作卡不得与层级条、右侧状态详情或最近执行重叠。当前无需执行的“确认接管系统控制”和“重试修复”等操作使用禁用样式且不可触发，并解释禁用原因。右侧状态详情不得重复全局底栏的控制状态、系统限制和今日计时；系统版本、主机型号、Atmosphère 检测结果分行显示。最近三条事件不得与排障摘要重叠或被截掉，手柄和触摸均可打开详情；PIN 认证后的家长区详情可展示设备标识、路径、原始事件名、请求 ID、错误码与内部状态，但任何页面仍不得显示完整 `grant_secret`、PIN、令牌签名材料或其他可复用秘密。无事件和读取失败必须分别提示。第六张卡为“软件信息”；手柄和触摸均可打开只读弹层，显示 `PlayWise（任我玩）`、当前构建版本、`https://github.com/selfuppen/NX-PlayWise` 和 `https://selfuppen.github.io/NX-PlayWise/`，按 `A`、`B` 或触摸关闭按钮均可返回。不得显示内部 Device Lab 信息。
10. 手柄和触摸均可操作数字键盘、额度和周计划；弹窗输入不穿透。触摸打开 PIN 验证时，原触摸必须先完全抬起，不能串入 PIN 数字、退格、取消、确认或传统键盘；抬起后的下一次独立触摸正常生效。NRO 以高频输入循环采样触摸，并把后台结果轮询和绘制节奏独立限频；连续快速点击不得因旧的 100ms 采样间隔漏掉。分别验证约 50ms、80ms 和 120ms 的短触碰、按钮边缘触碰、完全抬起后的连续触碰，以及触摸危险确认按钮约 1 秒；快捷键约 400ms、危险确认约 1 秒和 PIN 长按 `+` 调出系统键盘约 1 秒的真实时长不得随输入采样频率变化。快捷键预设、最近事件、节假日开关与模式等紧凑行的整行间隙应归入相邻可见控件，不得形成细小的不可触碰区域。
11. 状态语言一致：绿色“正常运行”、蓝色“正在同步”、黄色“兼容性待确认”、橙色“保护模式”；红色表示需要家长关注且会阻止正常控制的状态。保护模式显示“安全保护已触发”，主动停用显示“紧急停用已开启”，两者可并列。
12. `disable.flag` 下孩子页和全部家长页持续显示红色停用状态；status、诊断导出、解除停用和恢复仍可用，额度、周计划和加时码写入在 UI 与后台均被拒绝。分别以 `restored + disable.flag` 和 `active + disable.flag` 验证支持页显示“解除停用并重新接管”；以系统从旧 HOS 升级到新 HOS 形成的 `protection + runtime_fingerprint_changed` 验证引导页和支持页显示“系统环境已变化，重新检测并接管”，家长确认且只读预检通过后更新兼容指纹、保留现有配置和安装快照、删除 flag 并进入 5 秒宽限。其它 protection 停用原因仍不得由该入口穿透；预检失败保留 phase 与 flag。
13. 家长区所有页面底栏显示独立的 `Y 刷新` 与全局状态摘要；前四个按钮宽度为 `130 / 130 / 170 / 130px`，右侧状态栏从 `x=662` 延伸至主内容右边界 `x=1226`，不得与按钮重叠。两者均可触摸或从正文底部按方向键到达。正常、未加载、过期和普通读取失败状态按 `A` 或触摸状态摘要会刷新；保护、停用、临时解除或恢复事务按 `A` 直接进入“设置 → 支持与恢复”。家长主页面的 `Y` 始终只刷新，弹层中的 `Y` 保留输入清空等上下文语义，`L/R` 与 `B` 保持原导航语义。
14. 成功刷新超过 2 分钟后隐藏旧剩余分钟数并显示“状态待确认”；Nintendo 家长控制未启用、临时解除和读取不可用必须分别显示，不能把未知值显示成“否”。支持页不得再硬编码“到期行为：系统提醒”。
13. “导出诊断包”先提醒文件可能包含设备标识和文件路径，只应发送给可信支持人员；确认后的进行中、成功和失败状态均留在“支持与恢复”当前页，不得打开结果弹层。成功状态分三行完整显示导出路径、提交 GitHub Issue 时附加文件的说明和 `https://github.com/selfuppen/NX-PlayWise/issues`；离开支持页后清除。导出前刷新一次状态，确认 `runtime_snapshot` 包含 status 时间与年龄、计时/限制读数及其可用性、规则来源、紧急停用、活跃恢复事务、请求处理中和传输类型；未刷新时必须明确 `status_loaded:false`，不得把默认数字冒充实时读数。支持包中搜索 secret、PIN、已提交 8 位码、完整 nonce、私有 raw layout 和 recovery 原始内容均无匹配。
14. 进入/重新进入孩子页、切换到家长区“今日总额度”页以及每个时间调整入口都会主动刷新状态；页面显示 UTC+8 日期、当前已玩、今天还可玩、修改后预计还可玩和“上次同步 X 秒/分钟前”。“今天还可玩”在限时日直接显示 1454 的可靠读数，不限时显示“不限时”，读取失败显示“暂不可用”；超过 30 秒应能明显看出数据可能过期，超过 2 分钟隐藏旧数值并显示“状态待确认”。不限时日必须显示“已玩时间暂不可用”，不得从 PCTL 私有 1952 命令猜测；限时日只有在配置总分钟和 1454 剩余分钟均可靠时才显示已玩估算值，依赖已玩时间的“临时加时”在数据不可用时不可提交。
15. “设置今日总额度”“临时加时”、周计划额度、法定休假日额度和调休工作日额度共用 `920×620` 紧凑时长编辑框：左侧提供数字键盘与 -5/+5、-15/+15，右侧分别显示小时和分钟输入项并实时展示总分钟数及对应信息；所有可见按钮支持手柄和触控且不得文字重叠。默认聚焦分钟，触摸字段或手柄 Minus 可切换字段；首次输入替换当前字段，退格和清空不影响另一个字段，快捷增减按总分钟计算后同步两个字段。分钟分量只允许 0–59；验证普通额度的 1–1440 和临时加时的 1–120 边界，确认 `0:00`、分钟 60、`2:01` 临时加时及 `24:01` 普通额度均不能提交，最大值仅接受 `2:00` 和 `24:00`。无效中间输入显示“总计 -- 分钟”。今日总额度显示今天已玩、今天还可玩、修改后预计还可玩和刷新时间，并明确输入值是包含今天已玩时间的全天总额度；周计划编辑今天且存在今日临时设置时显示“今天当前不变；恢复周计划生效后预计还可玩 X”，没有临时设置时显示“保存后今天生效，预计还可玩 X”；编辑其他星期时明确今天不受影响。节假日编辑显示规则类型、启用状态、今日命中或下一次对应安排，今日日期命中但预设未开启或被临时设置覆盖时不得误报为正在生效。已玩时间可靠时，今日总额度默认取可靠的当前限时总额度与已玩时间中的较大值并钳制到 1–1440；当前总额度不可用且已玩为 0 分钟时使用 1。已玩不可用时沿用可靠的当前限时额度，否则使用 60，但必须说明这是编辑初始值而非保证剩余。今日总额度完成后提交或进入安全确认，其余页面完成后只更新草稿。恢复周计划确认框展示当前规则来源、当前临时额度、今天已玩、清除后生效来源和当天规则额度，并用“额度 - 已玩 = 剩余”说明计算依据；不限时或数据不可用时必须明确说明。不限时改限时使用状态卡突出“今天已玩 / 今天还可玩 / 修改后预计还可玩”。剩余 1–15 分钟使用黄色、0 分钟使用红色、不限时使用绿色，并始终保留文字标签。今日不限时、操作后为 0 或已玩不可用、无法判断是否立即限制时，触控不得绕过手柄长按 `A`；确认框分行显示立即限制风险和“今日不限时 / 临时加时 / 加时码”三种解除路径，且不得与状态卡或按钮重叠。`set_today_limit` 成功后，真实受限状态必须在最近执行中显示解除方式；只有剩余为 0 而未观察到 `restricted_now` 时，文案必须使用“可能即将生效”，不得伪造已经限制结论。
16. 周计划显示顺序固定为周一到周日，并在左侧 760px 区域横排 7 张 96×200 卡片。每张卡严格分为 42px 头部、50px 模式区和 108px 额度区：星期只在头部显示，“今天”位于模式状态下方的独立预留行；点模式区直接切换，点额度区在限时下打开紧凑分钟编辑框，不限时下提示先切模式，点头部或空白只同步焦点。批量操作通过底部按钮聚焦后按 `A` 或触控进入，复制最后选中日期的完整草稿到周一至周五或周六、周日，只修改草稿且不自动保存。方向键左右移动日期、向下进入 `X 切换模式 / 批量设置 / ZL 放弃 / + 保存` 按钮区，`A` 执行当前候选；在日期卡上 `A` 修改额度、`X` 切模式，`Y` 始终刷新。切回限时后恢复此前保留的分钟数。分钟输入标题使用“设置周X的周计划额度”，避免与今天的临时总额度混淆。右侧始终显示今天影响预估；没有有效变化时显示中性说明，仅修改其他星期时明确今天不受影响。没有 today override 时使用“今天还可玩 / 保存后预计还可玩”；存在 today override 时仍显示“今天还可玩 / 恢复后预计还可玩”，并始终提示保存不会清除今天的临时设置。限时/不限时切换和已玩时间未知都不得猜测。保存结果必须直接区分今日临时设置覆盖、节假日规则覆盖、今天已按新周计划生效或只修改其他日期，并展示生效来源、规则额度、已玩和预计剩余。无修改时放弃和保存仍可选中，但显示禁用状态并解释原因；有草稿离页时提供“保存 / 放弃 / 继续编辑”保护。
17. 紧急停用期间周计划保持可查看和可移动焦点，但模式切换、分钟输入、触摸增减、数字确认和保存全部使用禁用样式并在输入层与提交层同时拒绝；页面明确显示“紧急停用中，周计划暂时只读；解除停用后才能修改和保存”。停用前已有草稿不得自动丢失，仍允许放弃；编辑数字期间检测到停用时取消本次未确认输入并保留此前草稿。带草稿离开或刷新时不得提供必然失败的保存选项，改为“保留草稿 / 放弃草稿 / 返回”并默认保留；草稿只在本次 NRO 运行期间保留，退出或重启后不持久化。
18. 国家节假日页面使用 760px 顶部全局卡、左右并排的两张 368px 规则卡和底部 `X 切换模式 / ZL 放弃 / + 保存` 功能区；模式操作作用于最后选中的规则，默认法定休假。顶部只有开关热区切换启用状态，其余位置只选中；每张规则卡右上模式区由触控或 `X` 直接切换，主体额度区由触控或 `A` 打开紧凑分钟编辑框，不限时下给出先切模式提示。“查看当前节假日安排”位于右侧状态卡日历版本下方；其下固定展示“今日临时设置 > 国家节假日 > 周计划”及当前回退或命中原因。日历弹层的上一页、下一页、关闭具有独立焦点和触控区域，边界页禁用对应翻页按钮，`L/R` 仍可直接翻页、`B` 关闭。全局规则关闭时仍允许编辑预设；紧急停用时所有设置热区保持可命中以显示只读原因，但不得修改草稿，放弃与查看安排仍可用。国家节假日覆盖法定休假、调休补班、普通日期和未覆盖年份；今日临时设置始终优先。分别验证临时设置覆盖、法定休假当天、调休工作日当天、普通日期、总开关关闭和未覆盖年份；保存结果必须直接说明今天是否受影响；今天实际不变时只显示结论和简短原因，不显示额度、已玩或预计剩余计算；确实影响今天时继续显示生效来源、规则额度、已玩和预计剩余。保存后立即 enforce；儿童首页显示最终来源。覆盖结束前 30 天及进入未知年份后，家长区和儿童区均显示简洁告警，未知年份回退周计划。
    保存成功的 result 不得把本请求即将清理的 `recovery/active` 显示为待处理；真实的 Enforce 确认事务仍须显示。自制程序菜单高级入口等 Companion 本地配置操作完成后重新读取恢复状态，不得让旧告警残留在全局底栏。
19. “加时码”依次且仅显示“立即生成加时码”“手机/电脑生成加时码”“加时码生成管理”。前两个入口分别直达本机生成页和手机/电脑生成页，按 `B` 直接返回加时码页，不得出现旧聚合中转层或 `Y/X/+ /R/ZR` 隐藏快捷键。“加时码生成管理”使用两列五卡布局，依次提供管理加时码设备名、管理加时码密钥、导出手机/电脑配置、编辑二维码跳转地址和恢复二维码跳转默认地址；每项均可由方向键、`A` 和触摸执行，完成后留在管理页。恢复默认地址先验证 PIN，再显示包含默认地址的 `B 取消 / A 确认`。加时码页右侧帮助框不得与最近执行框重叠。“自制程序菜单高级入口”卡片只绘制一行动态说明，不得让静态副标题与状态说明重合；不显示 Toggle，只显示“已开启 / 未开启 / 外部配置 / 需要处理 / 状态未知”Badge 和详情入口。没有 PlayWise 事务与备份、但磁盘已经是目标配置时显示“外部配置”，说明入口可用但 PlayWise 无法恢复，不得报备份丢失或创建虚假前像；有 PlayWise 事务证据而备份缺失时才显示“需要处理”。详情弹层并列提供配置高级入口与恢复原启动方式，支持 `Y` 重新检测；不适用或状态未知的操作必须禁用。开启、恢复均默认聚焦取消并完整说明当前状态、目标状态、影响、操作方法和撤销办法；确认框标题固定为“配置自制程序菜单高级入口？”，开启说明必须明确重启后在桌面“手柄设置”图标上按住 `X`、再按 `A`，进入自制程序菜单（hbmenu），强制恢复仍需手柄长按确认。操作完成后留在详情弹层并重新检测真实状态，失败保持原状态并显示原因。高级入口配置刷新失败时须显示灰色未知状态并提示重新检测，不得保留旧状态冒充当前读数。
20. 扫码配对覆盖成功、缺少 fragment 参数、非法密钥、取消导入、已有配置覆盖，以及 `parent-import.json` 成功/失败路径都有确定性前端测试。
21. “手机/电脑生成加时码”标题与一级入口一致，并使用近全屏双栏：主推栏说明从完整交付包取得 `playwise-offline.html`，从管理页导出 `sdmc:/switch/playwise/parent-import.json`，传到可信家长设备后用系统浏览器打开并依次选择“导入配置文件”和“导入此设备”；同时提示手机不要使用聊天软件或网盘内置预览器。联网扫码栏作为可选便捷方式保留二维码，并完整显示最长 256 字节的合法当前地址，不得省略；二维码和配置文件均明确标为包含加时码密钥。页面只能使用 `B` 或触摸返回，不得按 `A/+` 意外关闭，也不得使用共享中文字体缺失的 emoji。
22. 本机生成器默认 20 分钟，六个时长调整按钮、生成和返回均可由方向键选中并按 `A` 执行；`L/R` 约 ±15、`ZL/ZR` 约 ±30、`+` 生成，并全部吸附到合法且不超过设备上限的档位。进入页面时主动刷新状态，并以状态卡显示“今天已玩 / 今天还可玩 / 兑换后预计还可玩”和更新时间；刷新失败时实时值显示暂不可用但仍允许生成。预计值受每日 1440 分钟上限约束并明确标出截断。未加载日期时拒绝生成，并排除当天 ledger 与本机已签发 nonce。调整时长或再次生成不得暗示已签发代码被撤销：当前代码继续显示其签发时长和生成时预计快照，并提示同日签发的其他代码可能仍可兑换。当天 512 个 nonce 均已签发或消费时必须拒绝继续生成，不得清空签发集合或复用 nonce。
23. 编辑二维码跳转地址提示只使用官方 PWA 或项目静态前端的可信自部署地址，并警告页面可以读取加时码密钥；运行时仍只做 URL 格式校验。覆盖 HTTPS、localhost/回环/私有 IP HTTP、path/query、恢复默认，以及 public HTTP、userinfo、控制字符、超长和已有 fragment 的拒绝路径。使用指南说明把完整 `tools/ptc_frontend` 发布到可信 HTTPS 静态站点即可满足扫码和文件导入能力，且 `python tools/ptc_frontend_server.py` 仅用于本机预览。
24. PWA 离线页面包含配对、码无效、日期、设备无响应、紧急停用和高级诊断排错说明。
    单文件 `playwise-offline.html` 还需直接从 `file://` 打开验证：页面完成算法自检、导入 `parent-import.json`、生成代码且不请求外部 JS/CSS/manifest/Service Worker。至少覆盖桌面 Chrome/Edge；Android 和 iOS 需记录文件管理器是否真正交给浏览器执行，不能把仅显示静态预览视为通过。
25. Overlay 顶部显示“自律约定 / 兑换加时奖励”；提交前顶部唯一显示“今天还可玩”和刷新时间，兑换成功后显示“修改后还可玩”，折叠栏和普通展开详情不得重复剩余时间或更新时间。不限时状态直接显示“不限时”。正式提交后读取结果期间，“今天还可玩”必须切换为橙色“正在刷新…”等待态，刷新按钮显示“确认中 / 提交后等待结果”，不得把旧分钟数继续画成实时值后再突然跳变。主题使用深黑面板、石墨卡片和暗青焦点底色；剩余时间使用带绿色强调线的 22px 深色内衬，最长 `1440 分钟`、刷新中和暂不可用均不得与刷新按钮或更新时间重叠。8 位输入槽为 40×48、数字字号 30，“已输入位数”字号 14，键盘数字字号 24；当前输入槽同时使用暗青底色和 3px 青绿边框，其他空槽保留中点；输入槽与“已输入位数”之间至少留出一个完整小字号高度。`X 退格` 使用琥珀边框，`Y 清空` 使用红色边框，且始终保留文字标签。预检的不限时转限时、兑换后为零和状态未知使用深红底、红框与红字；仅受每日上限截断使用深琥珀底、琥珀框与琥珀文字，危险状态优先于截断提醒。触摸命中区域必须与刷新、键盘、提交栏和动态高度状态栏的可见位置一致，不得存在可见但不可点或不可见却可点的区域，错误/成功展开状态栏不得越过页脚。打开时自动读取状态；空闲时 `Y` 和顶部触摸按钮可刷新。刷新或读取结果期间仍允许数字输入、方向移动、退格、触摸清空、展开/收起状态栏和按 `B` 关闭，只禁用会发起重叠请求的刷新、预览与提交；提交栏显示后台处理中但不得冻结本地输入。刷新期间保留输入和最近一次成功状态，成功后保持页面打开。刷新时间显示“刚刚刷新 / N 秒前”，30 秒后警示数据可能过期；不得用本地倒计时猜测剩余时间。status 成功不能按 `unlock_observed` 判为加时失败；加时码错误清空输入并提示重新输入，不提供会重交已提交代码的“重试”。
26. NRO 与 Overlay 都验证“输入 → 非消费预览 → 确认 → 再校验 → 正式兑换 → 结果”完整流程。预览不得写 PCTL、rules/state 或 ledger；取消确认、无效码、同步提交失败、PCTL/result/ledger 失败都不消费 nonce，并明确提示代码仍可使用。兑换后为 0、未知或从不限时改为限时时，NRO 必须支持手柄 `A` 或触摸按钮长按约 1 秒，并显示进度；移出、松开或取消应清零且完成只提交一次。Overlay 仍只接受手柄长按。正式提交后分别在 pending、processing、done/result 阶段强制退出并重启：界面只能恢复成功终态或持续显示“结果确认中”，不得重新提交；恢复标记不含完整代码，并且只在终态已展示、用户关闭结果后删除。

27. “临时加时”与“设置今日总额度”共用紧凑分钟编辑器，默认 15 分钟、范围 1–120，并显示今天已玩、今天还可玩、加时后预计和刷新时间。周计划批量弹层采用双栏：选择目标只更新预览，右栏按当前规则值分组并显示目标、改变、相同跳过数量；只有独立“应用到草稿”操作才复制规则，说明不得与返回或应用按钮重叠。孩子页家长快捷键区域必须完整显示最长预设组合与长按说明。

> NRO 危险确认的当前规则：操作后为 0、状态未知或不限时转限时时，支持手柄 `A` 或触摸确认按钮长按约 1 秒并显示进度；Overlay 仍只接受手柄长按。“临时加时”纳入同一紧凑编辑器，默认 15 分钟、范围 1–120。此规则替代上文遗留的“触控无效”表述。

## 资格验证

当前版本的资格验证基线：

- Nintendo Switch OLED；
- HOS 22.5.0；
- 资格测试环境 Atmosphère 1.11.2。

必须使用 `build/packages/` 中当前版本的标准安装包执行：

1. 在已启用官方时间限制的主机上安装并重启。
2. 确认首次只读预检完成，未确认前没有 PCTL 写入。
3. 创建一个自选长度的数字 PIN，确认自动生成的 secret 不等于任何公开占位值，确认一周规则。
4. 家长确认接管后，验证安装 snapshot、解除当前限制、5 秒同步和 Active 状态。
5. 分别测试 status、设置今日总额度、增加分钟、今日不限时、恢复周计划和周计划保存；另在额度已耗约 745 分钟时恢复 60 分钟周计划，确认系统限制出现后即使 1455 不再保持 true，请求仍成功、当天设置保持 60 分钟且不会回滚到原临时额度。
6. 使用一个由当前候选环境生成的 15 分钟 v2 加时码，确认 timer、剩余时间和 nonce 仅提交一次。
7. 制造一次状态传播延迟，确认显示“正在同步”；30 秒内确认成功或按契约回滚。
8. 重启验证 setup、rules、state、ledger 和未完成事务恢复。
9. 创建 `disable.flag`，确认只熔断写入；再验证诊断和安装快照恢复。
10. 恢复安装前 snapshot，确认原始 raw/timer 状态可被精确证明，并进入 `restored`。
11. 确认 `restored` 下首次设置第 3 步显示“解除停用并重新接管”；确认后必须先重新经过预检，成功时才解除 `disable.flag` 并在 5 秒宽限后进入 `active`，失败时必须保留停用标记和 `restored`，且安装 snapshot 不得被覆盖。
12. 制造一次 Enforce 状态传播延迟，确认 30 秒窗口内同一进程的 scheduler 不会把活跃 recovery 误判为启动遗留事务；随后在窗口内真实重启，确认遗留事务被恢复、创建 `disable.flag`，并可从 `active` 状态通过“解除停用并重新接管”安全恢复。
13. 设置次日为 60 分钟限时，在当天结束前让主机停留 HOME 后进入待机；次日保持屏幕和游戏未打开至少 90 分钟。确认 `pctl_debug.jsonl` 只有 Enforce 的 `apply_target`，没有 `stage:"start_timer"`，唤醒后 1454 剩余时间没有因后台跨日同步而减少，界面也不得显示虚假的 60 分钟额度消耗。
14. 分别记录 HOME、游戏前台、游戏挂起、待机和唤醒后的 1454 A/B。确认进入真实游戏后由 Horizon 启动或恢复倒计时，退出/挂起/待机不会继续消耗；若候选环境不能满足此项，不得把它标为新的资格基线。

当前版本已于 2026-08-10 在上述环境完成资格验证；manifest 的 `verified_environment.result` 为 `verified`。

## 失败取证

优先从标准分发构建的支持页导出脱敏诊断。若 NRO 无法启动，保留：

- `build.json`、`setup.json`、`compatibility.json`、`environment.json`；
- `backups/install_pctl_snapshot.json`；
- `recovery/active/`；
- `results/`、`logs/` 和相关 crash report。
- Overlay 或 NRO 显示 SD 传输时，检查当日 `logs/YYYY-MM-DD/ipc-client.log`；确认记录包含 `client`、`stage`、`rc`、`module`、`description`、`version` 和 `expected`，相同连接失败不会随每次刷新重复写入，且日志中不含请求正文、PIN、密钥或 8 位码。Overlay 进程启动后再提交一次状态刷新，正常连接必须记录 `client=overlay stage=connected`（此前发生过连接失败时）并在界面显示“传输：IPC”；退出并重新打开 Overlay 后重复验证，排除 SM 会话只在初始化阶段短暂有效。

不要对外发送 `credentials.json`、`auth.json`、完整 ledger 或原始离线码。要停止自动启动，可移除 `atmosphere/contents/4200000000BD2300/flags/boot2.flag`。
